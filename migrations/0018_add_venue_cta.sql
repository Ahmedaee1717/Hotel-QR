-- Migration 0018: Add custom CTA text for venue links
-- Allows admins to customize the button text for linked venues (e.g., "View Bars", "Explore Activities")

-- ALTER TABLE tier_benefits ADD COLUMN venue_cta_text TEXT DEFAULT 'View Details';
